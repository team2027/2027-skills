# Per-provider CLI login reference

Verified by probing each CLI (help + source + shadowed-`open` tests), and **most rows were completed end-to-end with a real human login this round** (Sanity, Netlify, Vercel, Cloudflare, Neon, Render, WorkOS, Railway, Daytona, Fly, Convex, Turso, Auth0, Stripe, Stack Auth, Modal, Supabase, + E2B already-authed; Clerk confirmed API-key-only). Method is in `SKILL.md`; this is the lookup table.

**Legend**
- 🟢 default `login` auto-opens the popup **and** stores the token itself — the happy path.
- 🔵 **device-code** — shows a `user_code`/URL to confirm in the browser (no localhost callback); surface the code to the human before they approve.
- ⌨️ **needs a real TTY** — bare/backgrounded login hits an interactive prompt and *fatals*. Drive it in a real-terminal foreground, with `npx noninteractive` (`<pkg>` for npm CLIs, `start <binary>` for binaries — then `send` the keystroke), or `expect`.
- 🔗 **prints the URL only** (no auto-open) — you must surface the URL to the human yourself.
- 🔑 **API-key-only** — no browser/OAuth flow exists; authenticate by setting/pasting an API key (e.g. Clerk: `clerk init` / `clerk config set clerk.key`). Here the `token` env var **is** the login path, not a fallback.
- `token` = the env var that skips the popup entirely (CI / headless / no-popup fallback).

The **Light install** cells append `--version` on purpose: it warms the npx/pip cache and prints the version **without side effects**. Running the bare package instead can *do something* — `npx -y vercel` deploys, `npx -y workos@latest` runs the installer — so never strip the trailing flag (see `SKILL.md`).

**`npx` doesn't leave the CLI on `PATH`.** The Login/Verify cells show the bare command (`sanity login`, `wrangler whoami`) for readability, but for an npx-install row on a fresh machine, run them the **same npx way** — `npx -y <pkg>@latest <subcommand>` (e.g. `npx -y @sanity/cli@latest login --provider google`, `npx -y wrangler@latest whoami`) — or `npm i -g <pkg>` once to get a real `PATH` binary. Bare `sanity`/`wrangler` after only an npx call is `command not found`. The `pip`/`brew`/`curl` rows *do* install a real binary, so their bare commands work as written (mind the PATH caveats noted per-row).

| Provider | Light install | Login command | Verify | Token env (CI) |
|---|---|---|---|---|
| Sanity 🟢 | `npx -y @sanity/cli@latest --version` | `sanity login --provider google` ⌨️*(picker → pass `--provider`)* | `sanity debug` → grep `Email:` *(multiline block; `User:` is just a header)* | `SANITY_AUTH_TOKEN` |
| Netlify 🟢 | `npx -y netlify-cli --version` | `netlify login` *(polls API, no localhost server)* | `netlify status` *(outside a linked project the exit code is unreliable — seen both `1` and `0` even when logged in, plus an `Error: not … linked to a project` line — so parse stdout for the identity block, don't trust the code; `--json` dodges the ANSI color codes in the plain output)* | `NETLIFY_AUTH_TOKEN` |
| Vercel 🔵 | `npx -y vercel --version` | `vercel login` *(device-code: auto-opens `…/oauth/device?user_code=XXXX`; show the human the code; unset `CI`)* | `vercel whoami` | `VERCEL_TOKEN` |
| Cloudflare 🟢 | `npx -y wrangler@latest --version` | `wrangler login` *(localhost:8976 callback. **As you launch, tell the human to be signed into dash.cloudflare.com with the right account** (seeds the session cookie the OAuth needs) — prime them in the *same* message, don't block waiting for a reply; most of the time they already are. Prints `Successfully logged in.` — only if it errors **`request_forbidden` / `No CSRF value available in the session cookie`**, the browser that opened the popup isn't signed into dash.cloudflare.com (or is blocking the cookie — Safari/Brave strict privacy): have the human log into the dash with the **right account** first, then reopen. A leftover config file ≠ a token — `whoami` is the truth)* | `wrangler whoami` | `CLOUDFLARE_API_TOKEN` (+`CLOUDFLARE_ACCOUNT_ID`) |
| Modal 🟢 | `pip install modal` *(lands `modal` off a bash call's PATH — invoke by full path, e.g. `~/Library/Python/3.x/bin/modal`)* | `modal setup` *(NOT `modal login`; auto-opens browser. Its output **leads with a benign `The modal command was not found on your path! … use python -m modal` banner — even when you invoked it by full path**; that's a self-check warning, NOT a failure — the login proceeds right below it, don't bail on it)* | `modal token info` | `MODAL_TOKEN_ID`+`MODAL_TOKEN_SECRET` *(no `modal logout` — clear state by deleting `~/.modal.toml`)* |
| E2B 🟢 | `npx -y @e2b/cli@latest --version` *(pin `@latest` — a bare `@e2b/cli` can run a stale copy from the project's own deps)* | `e2b auth login` *(short-circuits on a local token; even after `e2b auth logout`, completes **instantly with no visible popup** if the browser is already signed into e2b.dev — it auto-approves. Confirm via `e2b auth info`)* | `e2b auth info` | `E2B_API_KEY` *(`E2B_ACCESS_TOKEN` is deprecated — retired Aug 2026)* |
| WorkOS 🔵 | `npx -y workos@latest --version` | `workos auth login` *(NOT `workos login`; device-code: **auto-launches the browser** to `signin.workos.com/device` AND prints the manual URL + `Enter code: XXXX-XXXX` (a `Browser launch attempted…` line is normal — it's belt-and-suspenders) — surface the code regardless; remote poll, no localhost callback)* | `workos auth status` *(checks the device-flow creds in the keychain/`~/.workos/credentials.json`)* | `WORKOS_API_KEY` |
| Fly.io 🟢 | `brew install flyctl` / curl *(no npm pkg)* | `fly auth login` *(remote poll, no localhost callback → skip the port-collision check; auto-opens browser; prints `successfully logged in as <email>` on completion)* | `fly auth whoami` *(prints the **bare email** — or `--json` → `.email`; every `fly` cmd also prints `Warning: Metrics token unavailable … context canceled`, non-fatal noise — trust the exit code + that email, not output cleanliness)* | `FLY_API_TOKEN` |
| Render 🔵 | `brew install render` *(or per render.com/docs/cli)* — **new installs get v2.x** (browser OAuth, just works). *(Legacy only: brew's `render-oss` tap can stick at v1.x behind an untrusted-tap prompt — `brew trust render-oss/render`, or grab v2 from github.com/render-oss/cli/releases — asset `cli_<ver>_darwin_arm64.zip`, NOT `render-darwin-arm64`)* | `render login` *(browser OAuth: prints `Complete login … with code: XXXX-XXXX-XXXX-XXXX` + auto-opens `dashboard.render.com/device-authorization/<code>` → click Authorize. **v2.x auto-switches to text on non-TTY — no flag needed.** **After auth, v2 can prompt to set the active workspace** ("The CLI prompts you to set your active workspace") — but only on an *interactive* run; the backgrounded/non-TTY run this skill uses auto-switches to text and exits clean (`Login successful!`), so you'll usually never hit the picker (validated live). If an interactive run *does* park there, set it non-interactively with `render workspace set <ws-id> --confirm` (`render workspace current` shows the active one), then verify. Legacy v1.x: add `-o text` to dodge a post-login workspace-picker panic; never retry on that panic, just verify)* | `render whoami` | `RENDER_API_KEY` |
| Neon 🟢 | `npx -y neonctl --version` | `neonctl auth` *(a `CI` env var suppresses the browser → clear it (`CI= neonctl auth`) or set `NEON_API_KEY` for CI; a hidden `--force-auth` flag also works but is undocumented/unstable. prints `INFO: Auth complete`. No `neonctl logout` — clear state with `rm ~/.config/neonctl/credentials.json`)* | `neonctl me` *(**when logged out this triggers the popup itself** — *any* neonctl command auto-launches the OAuth flow with no creds, `neonctl auth` isn't special — so don't run `me` as a blind logged-out pre-check; **prime the human before the first neonctl command**. Once authed it prints the identity table; callback is a **random** high port, not `:4321`. And **don't pipe a logged-out neonctl command through `head`/`grep`** — the blocking callback-server login can SIGPIPE-die mid-approval, and a `timeout`-wrapped pre-check that gets killed leaves an **orphaned dead callback** the human's first Authorize lands on)* | `NEON_API_KEY` |
| Turso 🟢 | `brew install tursodatabase/tap/turso` / `curl get.tur.so/install.sh` *(curl installer lands in `~/.turso` — NOT on a bash call's PATH, invoke by full path)* | `turso auth login` *(unset `TURSO_API_TOKEN` first; localhost callback on a **random** port (`…?port=49817…`) → use `--headless` on remote/WSL; prints `✔ Success! Logged in as <username>`)* | `turso auth whoami` *(bare username handle, not an email)* | `TURSO_API_TOKEN` |
| Clerk 🔑 | `npx -y clerk --version` | `clerk init` *(interactive wizard: prompts for `CLERK_SECRET_KEY` from the dashboard, then `Would you like to save this to a named profile? (Y/N)` + a profile name — drive all three. Non-interactive: `clerk config set clerk.key <key>`, or `clerk init --dotenv` reads `CLERK_SECRET_KEY` from `.env`. **No browser/OAuth/popup — API-key only; `auth`/`login` are NOT clerk subcommands** (`clerk auth login` → `unknown command`))* | `clerk whoami` | `CLERK_SECRET_KEY` *(the actual CLI credential; stored in `~/.config/clerk/cli/profiles` (INI), not the OS keychain. No `clerk logout`; `clerk config unset clerk.key` works only on some versions — else delete `~/.config/clerk/cli/profiles`)* |
| Railway 🟢⌨️ | `npm i -g @railway/cli` *(need **v5+** — a brew/global at v5+ is fine; reinstall `--foreground-scripts` if `railway --version` is blank/<5: postinstall fetches the binary, skipped under `ignore-scripts`. Old v4.x's browser callback is flaky)* | `railway login` *(browser OAuth, needs a TTY. **Railway v5 opens its OWN browser** under a PTY with **zero keystrokes** (prints `Opening your browser to sign in`) → **`expect` is cleanest: one tab.** `npx noninteractive start railway login` works but **double-opens** — railway self-opens AND noninteractive auto-opens the same OAuth URL (two tabs of the same page) — so prefer `expect`. (If you must use noninteractive, `--no-open` is *meant* to suppress its auto-open, but it's unverified for this flow — railway opens at `start` time and noninteractive flags normally go on `send`/`read` — so don't rely on it without checking.) v5 auto-detects SSH/CI/no-DISPLAY and switches to device-code itself — pass `railway login --browserless` (pairing code) explicitly only when the box has no browser)* | `railway whoami` | `RAILWAY_API_TOKEN` |
| Daytona 🟢⌨️ | `brew install daytonaio/cli/daytona` / binary *(full tap formula — bare `brew install daytona` fails; no npm pkg → `npx noninteractive start daytona …` or `expect`)* | `daytona login` *(observed live: TUI `Select Authentication Method` → Enter on "Login with Browser"; the newer `daytonaio/cli` may open the browser directly with no picker — the `expect`/`noninteractive` driver handles either; prints `Successfully logged in!`. Daytona also prints a persistent **version-mismatch nag** on every command — its `…/daytonaio/daytona/releases.` URL gets auto-opened as a junk tab by `noninteractive`; the trailing `.` is noninteractive swallowing the sentence's period (bug #10), so it 404s — harmless, just close it)* | `daytona organization list` *(indirect check — succeeds vs auth-errors; prints no identity)* | `DAYTONA_API_KEY` (or `login --api-key`) |
| Convex 🔵⌨️ | `npx -y convex --version` | `convex login --device-name <name>` *(device-code: prints `…/device?user_code=XXXX`. `--device-name` skips only the FIRST prompt; a second `Open the browser? (Y/n)` gate ALSO needs a TTY — `--device-name` alone still fatals non-interactive, so drive it with `expect`/`noninteractive`. **Success prints `✔ Saved credentials to ~/.convex/config.json` and exits 0 — NOT a `logged in` string**, so detect completion via the `eof`→exit-code fallback (or match `Saved credentials`), never a `logged in` regex that never fires)* | `convex login status` *(shows teams, not email; it prints `Convex account token found in: ~/.convex/config.json` **even when logged out** — read the `Status: Logged in` line + the teams list, NOT the "token found" line)* | `CONVEX_DEPLOY_KEY` *(a deploy/admin key — authorizes CI **deploys**, NOT a user login; `convex login status` still reads logged-out with only this set)* |
| Supabase ⌨️ | **`npx -y supabase@latest --version`** *(use npx — a stale brew binary nags an upgrade URL every call that `noninteractive` then auto-opens as a junk docs tab; npx is always current)* | `supabase login --agent no` *(**v2.107+ auto-detects an agent → JSON mode** and exits `Cannot prompt for input in JSON output mode` *before* the Press Enter prompt — that JSON/agent mode is supabase trying to **refuse the browser and demand a `--token`**, the opposite of what this skill does. **`--agent no` overrides it to get the human's browser login** (the whole point — we're here precisely because there's no token yet). Needs a real TTY — bare/non-TTY fatals `Cannot use automatic login flow inside non-TTY environments`. **Paste-back flow — drive with `noninteractive` (npm form `npx noninteractive supabase login --agent no`, NOT `start <brew-binary>` — the brew nag URL becomes a junk tab), NOT a one-shot `expect`** (you must `send` a code back): `Press Enter to open browser…` → send Enter → the browser shows a verification code titled **"Enter this verification code in Supabase CLI"** → ask the human to relay it (as **text**, not a screenshot) → `noninteractive send <sess> <code>` **then `noninteractive send <sess> ''` (Enter) to submit** — the code alone won't commit → prints `Token cli_… created successfully.` + `You are now logged in. Happy coding!` (the older `Finished supabase login.` string is stale — match the `[exited 0]`/`[session complete]` markers, not a literal string). The code has a **~60s TTL** — minimize relay round-trips or it 404s (`Could not create CLI login session` → restart). If a relayed code returns **unauthorized**, the account hit its **access-token cap** → delete old tokens at supabase.com/dashboard/account/tokens (the cap can also block *session creation* — symptom: stuck at `Press Enter…`, browser shows `Could not create CLI login session`; same fix). Send the submit-Enter **only after** the buffer shows `Enter your verification code:` — a premature/second Enter trips a `404 / Retry`. `--token`/`SUPABASE_ACCESS_TOKEN` (PAT) skips the whole flow — the reliable path for unattended/headless agents. Logout: `supabase logout --yes` (bare fatals `context canceled` non-TTY). **Verified live (aleks@2027.dev).**)* | `supabase projects list` *(no `whoami`/email — identity = valid token + visible projects. It also prints `Cannot find project ref. Have you run supabase link?` on stderr **even when fully authed** — that's a benign no-linked-project note, NOT an auth error; ignore it)* | `SUPABASE_ACCESS_TOKEN` |
| Auth0 🔵⌨️ | `brew install auth0/auth0-cli/auth0` *(if `Cellar not writable`/no sudo: grab the Darwin arm64 tarball from github.com/auth0/auth0-cli/releases/latest. **If the GH API is rate-limited** (`API rate limit exceeded` → no asset URL from the `releases/latest` JSON), resolve the tag from the page redirect — `curl -sI …/releases/latest \| grep -i location` → `vX.Y.Z` — then fetch `auth0-cli_X.Y.Z_Darwin_arm64.tar.gz`. Run it by full path)* | `auth0 login` *(drive with `noninteractive`, **passing `--name auth0`** — `npx noninteractive start --name auth0 <full-path-binary> login` — because the session name auto-derived from a full path mangles into garbage and fails to start. **two** Enter sends: 1st picks `As a user` in the `How would you like to authenticate?` survey; CLI then prints a device code + `Press Enter to open the browser` → 2nd Enter opens `…/activate?user_code=XXXX-XXXX` (confirm the code). Ignore the 2 junk welcome tabs noninteractive auto-opens)* | `auth0 apis list` *(**not `auth0 tenants list` alone** — `tenants list` only reads config and shows a tenant "authenticated" even with an **expired** token; `apis list` exercises the token for an honest check. Clean state = `No tenants available. Use 'auth0 login' to add one` (v1.32+; older builds: `Failed to load tenants: config.json file is missing`); bare `auth0 logout` errors `Missing a required argument: Tenant` — use `auth0 logout <tenant>`)* | — *(no env token; CI = machine login `auth0 login --client-id … --client-secret … --domain …`)* |
| Stripe 🔵 | `npx -y @stripe/cli@latest --version` *(bin `stripe`; **`@stripe/cli` IS the CLI** — not bare `stripe`, which is the Node SDK. Good for Node-only/locked-down boxes; or `brew install stripe/stripe-cli/stripe`)* | `stripe login --non-interactive` *(non-TTY stdin auto-activates JSON mode → outputs `browser_url` + `verification_code` + `next_step`, then **exits 0 — step 1 of 2, NOT a completed login**. It doesn't auto-open itself, so **auto-open the `browser_url` for them** — `open '<browser_url>'` (macOS) / `xdg-open '<browser_url>'` (Linux): it's the safe `…/confirm_auth?t=…` confirm page. Show the `verification_code` too. **Open ONLY that `browser_url`** — never the `…/auth/cliauth…?secret=…` next_step URL (it returns **live keys** `rk_live_*`), which is exactly why you **don't** drive Stripe with `noninteractive` (it auto-opens *every* URL it finds, including the secret one). Then **run the `next_step` command verbatim**: `stripe login --complete '<next_step url>'` — its URL is the `…/auth/cliauth…?secret=…` **poll** URL, **NOT** the `browser_url`. It blocks ~130s. **Keep re-running `--complete` with that SAME url until it captures** — whichever poll is running *at/after* the human approves is the one that saves the key, so a one-shot that times out *before* they approve **strands** the approval (loop `--complete` back-to-back across their approval window — **no idle gap**: each call exits `exceeded max attempts` after ~130s, and an approval that lands while *nothing* is polling is caught by neither run (so don't re-arm once and idle — expect several back-to-back runs; the approval is durable server-side until one captures)). Do NOT restart `--non-interactive` (a fresh url orphans it). **Do NOT drive Stripe with `noninteractive`/`expect`** — there's no keystroke to send (stripe exits immediately), and worse, `noninteractive` auto-opens **every** URL it finds, *including* the `…cliauth…?secret=…` poll URL, which on approval returns **live API keys** (`rk_live_*`): opening it in a browser leaks the secret to history/extensions and can consume the one-time response before your `--complete` poll gets it. That secret URL must stay inside your `--complete` process only)* | `stripe get /v1/account` *(authenticated API call — `config --list` passes even logged-out; exit 0 after the login JSON ≠ success, always verify here)* | `STRIPE_API_KEY` / `--api-key` |
| Stack Auth 🔗⌨️ | `npx -y @stackframe/stack-cli@latest --version` | `stack login` *(**buffers the URL on non-TTY stdout** — even `stdbuf -oL` won't flush it, so run under a PTY (`expect`/`noninteractive`) to surface it. Prints a URL + login code with a **~2 min TTL** — approve fast; be pre-logged-in to app.stack-auth.com or the redirect eats the TTL. Polls until approved; keep alive (`nohup`). NOT `@stackframe/init-stack`)* | `stack whoami` | `STACK_CLI_REFRESH_TOKEN` |

## The ⌨️ TTY-gated logins (where a bare/backgrounded run fatals)

These show an interactive prompt *before* the popup, so a non-TTY run errors out. Run them in a real-terminal foreground, via `npx noninteractive` (`<pkg>` or `start <binary>`) or `expect`, sending the keystroke:

- **Railway** — needs a TTY (non-TTY: `Cannot login in non-interactive mode`); v5.x auto-opens under a PTY with no keystroke (old v4.x prompts `Open the browser? (Y/n)`). v5 auto-detects SSH/CI/no-DISPLAY and switches to device-code itself — pass `railway login --browserless` (pairing code; also needs a TTY) explicitly only when the box has no browser.
- **Daytona** — TUI `Select Authentication Method` → `Login with Browser` (press Enter).
- **Render** *(v1.x only)* — default `--output interactive` runs a post-login workspace picker that fatals non-TTY (`panic: could not open /dev/tty`); the token is saved BEFORE the panic, so a backgrounded `render login` still succeeds — pass `-o text` to skip it, then confirm the printed device code. **v2.x auto-switches to text on non-TTY**, so no panic (the `-o text` flag stays harmless).
- **Auth0** — survey `How would you like to authenticate? As a user / As a machine`, then `Press Enter to open the browser`.
- **Stripe** *(NOT Enter-gated — non-TTY auto-emits JSON and exits 0, even under a PTY: the npm shim's stdin isn't seen as a terminal)* — **don't drive it with `expect`/`noninteractive`**: there's no keystroke to send, and `noninteractive` would auto-open the `…cliauth…?secret=…` poll URL — a URL that returns **live API keys** (`rk_live_*`) on approval and must NOT be opened in a browser (leaks the secret; can consume the one-time response). Just run `stripe login --non-interactive`, **`open` the `browser_url`** (the safe `confirm_auth` page — open ONLY that, never the secret next_step URL) + show the `verification_code`, then `stripe login --complete '<next_step url — the cliauth?secret= one>'`, re-arming until it captures. Verify with `stripe get /v1/account`. (Full detail in the Stripe row above.)
- **Supabase** *(paste-back — use `noninteractive`, not a one-shot `expect`)* — needs a TTY (non-TTY fatals `Cannot use automatic login flow inside non-TTY environments`). `Press Enter to open browser…` → send Enter → the browser shows a verification code to **enter back into the CLI** ("Enter this verification code in Supabase CLI") → relay it (text, not a screenshot) via `noninteractive send <sess> <code>` **then send Enter** to submit (~60s TTL; if `unauthorized`, the account hit its token cap → delete old tokens). (`--no-browser` instead prompts `Enter your access token` for a PAT; skip with `--token`/`SUPABASE_ACCESS_TOKEN`.)
- **Convex** — TWO gates: a `Device name:` prompt (skip with `--device-name <name>`) **and** a following `Open the browser? (Y/n)` prompt that also needs a TTY, so `--device-name` alone still fatals non-interactive. Drive the `Open the browser?` answer with `expect`/`noninteractive` — the generic `expect` recipe's `open the browser` match already handles it.
- **Sanity** — only the provider picker is interactive; `--provider google|github` skips it and the rest is hands-free.
- **Stack Auth** *(not a prompt, but ⌨️-gated all the same)* — it **buffers the login URL on non-TTY stdout** (even `stdbuf -oL` won't flush it), so run under a PTY (`expect`/`noninteractive`) to surface the URL.

## Common popup-suppressors to NOT set

`CI=1` (Vercel, WorkOS, Neon → URL prints but no popup) · `BROWSER=none` / bogus `$BROWSER` · `--no-open` / `--no-browser` / `--headless` / `--browserless` / `--non-interactive` · a token env var already set (most CLIs skip login when their `*_TOKEN`/`*_API_KEY` is present — unset it to force the popup). Stored creds in the CLI's *config file* (not just env) can also short-circuit login or block verify (e.g. Daytona's `config.json` `api.key`) — clear those too if a login mysteriously no-ops. Daytona's config is `~/Library/Application Support/daytona/config.json` (macOS) / `~/.config/daytona/config.json`; token = the profile's `api.token` (NOT the keychain), and `daytona logout` clears the token but leaves a stale `api.url` — a leftover `http://localhost:3000` makes login *succeed* yet `daytona organization list` fail `connection refused` (delete the config to reset).

**Exception — Turso `--headless`:** the blanket `--headless` ban above is the rule for auto-open CLIs, but Turso *documents* `turso auth login --headless` as its WSL/remote/SSH fallback — it prints the auth URL to open manually instead of trying (and hanging on) a browser auto-open. In exactly the headless/agent contexts this skill targets, prefer it over fighting the auto-open.

**Exception — Railway `--browserless`:** the Railway row offers `railway login --browserless` (pairing code) as the no-browser fallback, so it's exempt from the `--browserless` ban above. **Caveat:** it's been **flaky for agents** (may not open a popup) — last resort only, and re-verify with `railway whoami`.

**Exception — Stripe `--non-interactive`:** the `--non-interactive` ban targets CLIs where the flag *bypasses* the browser login. Stripe doesn't skip auth — `stripe login --non-interactive` forces the JSON payload (`browser_url` + `verification_code` + `next_step`) you relay to the human, then you finish the real login with `stripe login --complete '<url>'`. (On a non-TTY stdin Stripe auto-selects that JSON mode anyway — the flag just makes it explicit; either way Stripe does **not** auto-open, so you surface the URL yourself.) So Stripe is exempt — it's the command the row needs to get the relay payload, not a popup-suppressor.

## Install docs (fresh-machine links)

Verified official install pages — reach for these when a CLI isn't already on the box. The table's `npx`/`pip` forms install-on-run; the `brew`/`curl` binaries (Fly, Render, Turso, Daytona, Auth0) need a real install first. *(Supabase and Stripe ship brew binaries too, but their rows use the npx form — Supabase because the stale brew build nags an upgrade URL that `noninteractive` auto-opens as a junk tab, Stripe because `npx -y @stripe/cli@latest` works fine (handy on Node-only boxes) — so neither sits in this binary list.)*

- Sanity — https://www.sanity.io/docs/apis-and-sdks/cli
- Netlify — https://docs.netlify.com/api-and-cli-guides/cli-guides/get-started-with-cli/
- Vercel — https://vercel.com/docs/cli
- Cloudflare — https://developers.cloudflare.com/workers/wrangler/install-and-update/
- Modal — https://modal.com/docs/guide
- E2B — https://e2b.dev/docs/cli
- WorkOS — https://workos.com/docs/authkit/cli-installer
- Fly.io — https://fly.io/docs/flyctl/install/
- Render — https://render.com/docs/cli
- Neon — https://neon.com/docs/reference/cli-install
- Turso — https://docs.turso.tech/cli/installation
- Clerk — https://clerk.com/docs/cli
- Railway — https://docs.railway.com/cli
- Daytona — https://www.daytona.io/docs/en/tools/cli/
- Convex — https://docs.convex.dev/cli
- Supabase — https://supabase.com/docs/guides/local-development/cli/getting-started
- Auth0 — https://auth0.com/docs/deploy-monitor/auth0-cli
- Stripe — https://docs.stripe.com/stripe-cli/install
- Stack Auth — https://docs.stack-auth.com/docs/js/getting-started/setup
